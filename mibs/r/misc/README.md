# MIB Repository

Place vendor or custom MIB files in this directory so the polling agents can parse
`sysObjectID` trees and translate OIDs to human readable descriptions. The path is
configurable via the `MIB_PATHS` setting or `MIB_PATHS` environment variable.
